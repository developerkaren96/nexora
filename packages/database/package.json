{
  "name": "@nexora/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push && pnpm rls:apply",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy && pnpm rls:apply",
    "db:studio": "prisma studio",
    "db:seed": "tsx src/seed.ts",
    "rls:apply": "tsx src/rls.ts"
  },
  "prisma": {
    "schema": "./prisma/schema.prisma",
    "seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "argon2": "^0.41.1"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "@types/node": "^22.7.4"
  }
}
